[project]
name = "rfm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "packaging>=25.0",
    "pyyaml>=6.0.2",
    "safetensors>=0.6.2",
    "torch>=2.8.0",
    "torch-geometric>=2.6.1",
]
